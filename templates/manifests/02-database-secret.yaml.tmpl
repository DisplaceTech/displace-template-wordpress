apiVersion: v1
kind: Secret
metadata:
  name: {{.ProjectName}}-mariadb
  namespace: {{.Namespace}}
  labels:
    app.kubernetes.io/name: {{.ProjectName}}
    app.kubernetes.io/instance: {{.ProjectName}}
    app.kubernetes.io/component: database
    app.kubernetes.io/managed-by: displace
type: Opaque
data:
  # PLACEHOLDER VALUES - DO NOT USE IN PRODUCTION
  # Actual values are in secrets/database-secret-override.yaml (git-ignored)
  # Apply the override file during deployment
  root-password: Y2hhbmdlLW1l  # base64('change-me')
  password: Y2hhbmdlLW1l       # base64('change-me')
